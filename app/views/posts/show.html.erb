<h3>Show Post</h3>
Post: <%= link_to @post.title, fix_url(@post.url) %>

<h3>Comments</h3>

<%= form_for [@post, @comment] do |f| %>
	<%= f.label :body, "Leave a Comment" %>
	<%= f.text_area :body %>
	<br>
	<%= f.submit "Create Comment", class: "btn btn-primary" %>
<% end %>


<ul>
	<% @post.comments.each do |comment| %>
	<li><%= comment.body %></li>
	<% end %>
</ul>

